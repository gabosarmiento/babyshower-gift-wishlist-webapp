<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) %>
    <script>
      $('body').append(noty({layout: 'topCenter' ,text: "<%= j msg %>", type: "<%= j name.to_s == 'notice' ? 'success' : 'danger' %>", timeout: 5000}));
    </script>
  <% end %>
<% end %>