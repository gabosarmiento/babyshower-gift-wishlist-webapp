<h3>Regalos de la lista <strong><%= link_to @lista.nombre.to_s.humanize.titleize, [@fiesta, @lista]
 %></strong></h3>
<% if policy(@lista).create? %>
<%= link_to "Añadir Regalo", new_fiesta_lista_regalo_path(@fiesta, @lista), class: "btn btn-primary" %>
<% end %>
<hr>
<%# render @regalos %>
