<h1>Nueva Lista</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guías para las listas</p>
    <ul>
      <li>Asegurate de que el nombre sea el indicado.</li>
      <li>El nombre debe tener mínimo 5 caracteres</li>
      <li>La Categoria solo será visible para todos los que invites y todos podran ver los regalos en esta lista</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @lista, :url => listas_path, html: {class: "lista-form"} do |f| %>
    <% if @lista.errors.any? %>
    <div class="alert alert-danger">
        <% @lista.errors.full_messages.each do |msg| %>
          <div><%= msg %></div>
        <% end %>
    </div>
    <% end %>
    <%= control_group_tag(@lista.errors[:nombre]) do %>
      <div class="form-group">
        <%= f.label :nombre %>
        <%= f.text_field :nombre, class: 'form-control', placeholder: "Ingresa el nombre de tu lista: lista de bodas, lista de regalos..." %>
      </div>
    <% end %>
      <%= f.submit "Guardar", class: 'btn btn-success' %>
    <% end %>
  </div>
</div>